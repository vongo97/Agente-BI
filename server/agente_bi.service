[Unit]
Description=Streamlit Agente BI
After=network.target

[Service]
User=root
WorkingDirectory=/root/agente_bi
ExecStart=/root/agente_bi/venv/bin/streamlit run app.py --server.port 8501 --server.address 0.0.0.0
Restart=always

[Install]
WantedBy=multi-user.target
